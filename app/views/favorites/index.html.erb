<div class="container">
  <div class="row justify-content-center">
    <h1>Tus peliculas favoritas son:</h1>
    <table class="table table-striped table-hover ">
      <thead>
        <tr>
          <th>#</th>
          <th>Imagen</th>
          <th>Nombre</th>
          <th>Año</th>
          <th>Puntaje</th>
        </tr>
      </thead>
      <tbody>
        <% @favorites.each do |f| %>
          <tr class="success">
            <td><%=f.id %></td>
            <td><%= image_tag(f.cover, :width => 65, :height => 75) if f.cover.present? %></td>
            <td><%=f.title%></td>
            <td><%=f.year%></td>
            <td><%=f.rating%></td>

          </tr>
        <%end%>
      </tbody>
    </table>
  </div>
</div>
